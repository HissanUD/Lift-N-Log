[project]
name = "fast-api-prac"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "pydantic>=2.13.4",
]

[tool.fastapi]
entrypoint = "main:app"
